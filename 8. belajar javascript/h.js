function getFixedCounter(k) {
    let initialValue = 0
    let result = []
    
    console.log(initialValue = initialValue + k)
    console.log(initialValue = initialValue - k)
}

getFixedCounter(1);